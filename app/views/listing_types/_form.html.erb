<%= form_with(model: listing_type, local: true) do |form| %>
  <% if listing_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(listing_type.errors.count, "error") %> prohibited this listing_type from being saved:</h2>

      <ul>
      <% listing_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :listing_type_name %>
  </div>

  <div class="field">
    <%= form.label :icon %>
    <%= form.file_field :icon, id: :listing_type_icon %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
