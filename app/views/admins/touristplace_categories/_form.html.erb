<%= form_with(model: touristplace_category, local: true) do |form| %>
  <% if touristplace_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(touristplace_category.errors.count, "error") %> prohibited this touristplace_category from being saved:</h2>

      <ul>
      <% touristplace_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :touristplace_category_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
